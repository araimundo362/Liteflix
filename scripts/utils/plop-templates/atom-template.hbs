import { atom } from 'recoil';

export const {{ camelCase atomName }}State = atom({
  key: '{{ camelCase atomName }}State', // unique ID (with respect to other atoms/selectors)
  default: undefined, // default value
});