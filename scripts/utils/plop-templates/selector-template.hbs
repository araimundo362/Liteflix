import { selector } from 'recoil';

const {{camelCase selectorName}} = selector({
  key: '{{camelCase selectorName}}', // unique ID (with respect to other atoms/selectors)
  get: ({get}) => {
    return '{{camelCase selectorName}} works!'
  },
});

export default {{ camelCase selectorName }};